on:
  push

jobs:
  build-website:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y python3-setuptools python3-numpy python3-flake8 python3-sympy python3-pip
        name: Install dependencies
      - uses: actions/checkout@v2
      - run: ls /usr/local/lib/python*
      - run: |
            git clone https://github.com/mscroggs/feast.git feast-src
            cd feast-src
            sudo python3 setup.py install
        name: Install feast
      - run: ls
        name: ls
